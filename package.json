{
  "name": "hivex-data-aggregator",
  "version": "1.0.0",
  "description": "Data aggregation for HiveX AI security projects",
  "main": "scripts/aggregate-data.js",
  "scripts": {
    "aggregate": "node scripts/aggregate-data.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "fs-extra": "^11.1.1"
  },
  "devDependencies": {},
  "keywords": ["ai", "security", "aggregation", "hivex"],
  "author": "AI&Me",
  "license": "MIT"
}
